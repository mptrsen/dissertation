#!/bin/sh
basedir=$(dirname $0)
cd "$basedir"
cd ..

dissertation="$1"

# Build the dissertation.
xelatex "$dissertation"
bibtex "$dissertation"
xelatex "$dissertation"
xelatex "$dissertation"

# Don't hide the log.
#mv "$dissertation.log" ".logged"

